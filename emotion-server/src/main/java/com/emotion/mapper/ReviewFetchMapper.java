package com.emotion.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.emotion.entity.ReviewFetch;

/** {@code t_review_fetch} 读写口，按 {@code trade_date} 唯一，走 BaseMapper 即可。 */
public interface ReviewFetchMapper extends BaseMapper<ReviewFetch> {
}
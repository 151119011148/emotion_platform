# 主线与日内核心 · 双轨页 字段与接口定义

> 评审稿 v0.2 · 2026-09-13（v0.1 修订：敲定 §6 三条决策）
> 范围：`mainline` 页改为「主线区（已确认，打 D2 分）+ 雷达区（候选池，不打 D2 分）」双轨。本稿定字段、接口与 schema，不改打分引擎实现。
> 全部依赖真实实现：`PrdMetricsService`(取数) → `MainlineService`(组装) → `/api/prd/mainline`(端点) → `MainlineView.vue`(渲染)；D2 封顶在 `BoardScoreCalculator.applyThemeMainGuards`。

---

## 0. 核心语义（本次改动的地基）

「日内核心」与「主线」不是合并关系，是**晋级关系**：

```
雷达区（当日最强板块扫描，候选池）──连续≥3个热度交易日──▶ 主线区（已确认，打D2分+生命周期）
   元件(1天🆕)                                            旅游(5天·扩散)  D2=88
   电力(1天🆕)                                            半导体(4天·扩散)  D2=71
```

- **候选池**：当日所有涨停家数≥1 的行业，只排强度 + 标连续天数，**不打 D2 分**。
- **晋级结果**：连续热度≥3 天的行业（或人工主线标记），才是 **D2 评分对象**，走五要素 + 生命周期天花板。

### 0.1 评分对象选择（当前代码与此不符，最需要对齐的一处）

现状（`PrdMetricsService.aggregate`）把 **当日涨停聚集度最高的行业** 直接当作 `mainIndustry`，D2 就评它。
但按双轨语义，**一件只热 1 天的板块（元件）不具备生命周期，不该成为 D2 主线**。判定分两层：

```text
radarTop  = 当日涨停聚集度最高的行业            （日内核心，仅雷达区，可只有1天）
mainline  = 连续≥3天 的行业中最强者              （已确认主线；若无任一≥3天 → 无主线）
```

- **`mainline` 非空**（存在 ≥3 天行业）→ 主线区评 `mainline`，走生命周期天花板。
- **`mainline` 为空**（今日最强者 <3 天）→ 「无主线」，主线区评 `radarTop`，**D2 封顶 40** 并打标 `⚠️无主线，仅日内核心炒作`。

> 注意：`mainline` ≠ 今日最热。例子里今日 元件(9家/1天) 比 旅游(6家/5天) 热，但 D2 评的是 **旅游**（已确认主线），元件只在雷达区标 🆕。
> 这意味着 `mainlineConfirmed == true` 时，主线区展示的行业可能**不是**今日聚集度最高者。字段层需要同时暴露两者。

---

## 1. 数据模型 / VO 字段

### 1.1 新增：`RadarRow`（雷达区一行）

```java
@Data class RadarRow {
    String industry;          // 行业（东财 hybk）
    Integer zt;               // 当日涨停家数
    Integer ztGatherPct;      // 涨停聚集度 %（本行业涨停 / 全市场涨停）
    Integer maxBoard;         // 板块内最高连板
    Integer persistenceDays;  // 连续热度天数（口径同主线：当日该行业Zt≥5 往前数，见 §3）
    String flag;              // NEW(1天🆕) / WATCH(2天) / MAIN(≥3天⭐) / MANUAL(人工主线)
    Boolean isMainline;       // 是否已收集为主线（persistenceDays>=3 或 人工标记）
    MainlineVO.Member leader; // 板块内最高板（可null），可点击升级到主线区
}
```

**flag 判定**（与主线确认口径统一，阈值 `HOT_ZT_THRESHOLD=5`）：

| persistenceDays | flag | 展示 |
|:--|:--|:--|
| 1 | `NEW` | 1天 🆕，仅观察，不参与 D2 |
| 2 | `WATCH` | 2天，接近临门一脚 |
| ≥3 | `MAIN` | 连续N天 ⭐主线，可升级/点击进主线区 |
| 人工标记 | `MANUAL` | 用户手标主线 |

### 1.2 `MainlineVO` 扩展字段（向后兼容，前端逐步切换）

```java
class MainlineVO {
  // —— 主线区（已确认）——
  String   mainIndustry;          // 语义改为「D2 评分对象」= mainline（≥3天最强者，或人工标记行业）；无主线则=radarTop
  Boolean  mainlineConfirmed;     // 沿用：persistenceDays>=3 或 该日有该项的人工主线标记
  Boolean  hasMainline;           // 新增：radar 任一 row.isMainline 为真 或 当日存在 t_mainline_mark 人工标记
  Boolean  manuallyMarked;        // 新增：当前 D2 对象是否由人工 t_mainline_mark 标记产生（前端标 🏷人工）
  String   mainlineSignal;        // 新增打标：无主线→"⚠️无主线，仅日内核心炒作（最强板块仅N天）"；有主线=null
  String   radarTopIndustry;      // 新增：radar 当日聚集度最高行业（与 mainIndustry 可不同，供前端展示错位）
  List<String> lifecycle;         // 沿用轨道
  String   lifecycleStage;        // 沿用
  DateTimeKey... // 五要素/龙头分工/轮动全部沿用，但对象从「mainline」出
  // —— 雷达区（候选池，新增）——
  List<RadarRow> radar;           // 按 zt 降序、persistenceDays 降序
}
```

> 五要素 / 龙头分工 / 生命周期 的取值对象统一改为 **mainline**（§0.1）。龙头分工字段沿用 `dragon/zhongJun/genFengCount/kaWei/fanBao/sectorLeader/dragonAligned`，但计数基数从 mainline 板块出，并加 §4 去重。

---

## 2. 接口定义

### 2.1 复用并扩展 `GET /api/prd/mainline`

不新增端点；让双轨页面一次取数（雷达聚合复用主线程已加载的 `dailyIndustryZt`，不额外撞 DB）。

**请求**（沿用）
```
GET /api/prd/mainline?date=2026-09-11
```

**响应体（JSON 关键段）** —— 组件例：主线=旅游(5天)，雷达今日榜首=元件(1天)

```json
{
  "tradeDate": "2026-09-11",
  "mainIndustry": "旅游",          // D2 评分对象 = 已确认主线
  "radarTopIndustry": "元件",      // 今日聚集度最高（候选）
  "mainlineConfirmed": true,
  "hasMainline": true,
  "mainlineSignal": null,          // 无主线时如 "⚠️无主线，仅日内核心炒作（最强板块仅1天）"
  "lifecycleStage": "扩散",
  "ztGatherPct": 12.5,
  "heightGatherPct": 50.0,
  "amountGatherPct": 33.1,
  "catalystHardness": 3,
  "persistenceDays": 5,

  "dragon":    { "code":"600000","name":"XX","industry":"旅游","board":5, "action":"HOLD" },
  "sectorLeader": { "code":"600001","name":"YY","industry":"旅游","board":4 },
  "dragonAligned": true,
  "zhongJun":  [ {"code":"600002","name":"ZZ","board":2} ],
  "genFengCount": 4,
  "kaWei":     null,
  "fanBao":    [ {"code":"600003","name":"WW","board":1} ],
  "rotationSignals": ["新题材种子：元件(9)"],

  "radar": [
    { "industry":"元件","zt":9,"ztGatherPct":34.6,"maxBoard":2,"persistenceDays":1,"flag":"NEW","isMainline":false,
      "leader": {"code":"000001","name":"A","board":2} },
    { "industry":"旅游","zt":6,"ztGatherPct":23.1,"maxBoard":4,"persistenceDays":5,"flag":"MAIN","isMainline":true,
      "leader": {"code":"600000","name":"XX","board":5} },
    { "industry":"电力","zt":5,"ztGatherPct":19.2,"maxBoard":2,"persistenceDays":1,"flag":"NEW","isMainline":false,
      "leader": {"code":"000002","name":"B","board":2} }
  ]
}
```

**前端渲染对应**
- 上半区（主线区）：`mainIndustry` + `lifecycleStage` + 五要素 + 龙头分工（评分对象=主线）+ `mainlineSignal` 打标；`manuallyMarked==true` 时在主线名旁标 🏷人工。
- 下半区（雷达区）：`radar` 表格。`flag=="MAIN"` 行高亮 ⭐；`flag=="NEW"` 标 🆕 只观察。任一雷达行点「升级到主线区」→ 调 §2.3 写接口落库人工标记；已人工标记的行显示「取消升级」。
- 命名单一化（顺带修硬伤3）：走势图标题 `D2主线明确度走势`，`MainlineView.vue:16` 的 `name` 与 h2 统一改；曲线数据不变（`scoreThemeMain`）。

### 2.3 人工主线标记接口（决策点1=落库）

雷达区点击「升级到主线区」调写接口，落 `t_mainline_mark`，服务端重取该日 mainline 返回更新后的双轨数据。

```java
// 升级（写人工标记）
POST /api/prd/mainline/promote
body: { "tradeDate": "2026-09-11", "industry": "元件" }
→ 200 { code:0, data: <重新 get /api/prd/mainline 的完整 MainlineVO> }
  语义：若该日已有人工主线标记，本次覆盖为新 industry；hasMainline=true，D2 评被标记行业（走生命周期，不封40）。

// 取消升级（删人工标记）
DELETE /api/prd/mainline/promote
body: { "tradeDate": "2026-09-11", "industry": "元件" }
→ 200 { code:0, data: <更新后的 MainlineVO> }
  语义：删除该 user+date+industry 的人工标记；若删后该日无任何≥3天行业且无其它人工标记 → 回到「无主线」，D2 封顶40。
```

**落库表**：

```sql
CREATE TABLE t_mainline_mark (
  id        BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id   BIGINT       NOT NULL,
  trade_date DATE        NOT NULL,
  industry  VARCHAR(64)  NOT NULL,
  manual    TINYINT      NOT NULL DEFAULT 1,
  created_at DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_user_date_industry (user_id, trade_date, industry)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

- `hasMainline = radar 任一 isMainline 为真 === OR === (SELECT COUNT(*) FROM t_mainline_mark WHERE user_id=? AND trade_date=? ) > 0`。
- `manuallyMarked`：D2 评分对象由本表产生时 true。人工标记优先级 **高于** ≥3天自动主线（用户显式选择优先）。
- 新增：`MainlineMark` entity + `MainlineMarkMapper`；`MainlineService` 注入后组装 `hasMainline/manuallyMarked/评分对象选择`。

### 2.4 `RadarRow` 连续天数取数

复用 `PrdMetricsService.aggregate` 已建好的 `dailyIndustryZt`（日期→行业→涨停家数，窗口 30 日，一次 SQL 已拉全）。把现主线单行业回看逻辑 `persistence 计算`抽成公共方法：

```java
// 抽公共：任意行业的连续热度天数（当日该行业 ZT>=5 往前数，走 PERSISTENCE_WINDOW）
static int consecutiveDays(Map<LocalDate, Map<String,Integer>> dailyIndustryZt,
                           LocalDate date, String industry) {
    int days = 0; LocalDate c = date;
    for (int i=0;i<PERSISTENCE_WINDOW;i++){
        Integer n = dailyIndustryZt.get(c) == null ? null : dailyIndustryZt.get(c).get(industry);
        if (n==null || n < HOT_ZT_THRESHOLD) break;
        days++; c = c.minusDays(1);
    }
    return days;
}
```

雷达区仅对**当日有涨停**的行业算（集合小），主线字段走同一方法，彻底消除双口径。

---

## 3. 龙头去重算法（设计稿，P0）

**原则：一只票全页只允许一个角色。** 优先级 `总龙头 > 中军 > 卡位 > 反包 > 跟风`。当前代码的机器快照链路已结构性去重（总龙独立卡、中军 `row.equals(zongLong)` 排除、跟风是计数），这里补上两处真实漏洞并写成显式算法：

```text
roles = {}                      # code -> 角色
if 总龙头存在: roles[总龙头.code] = "总龙头"
for 中军候选(按板数降序): if code 不在 roles 且 board>=2 且 属 mainline 板块:
    roles[code]="中军"
卡位 = 非 mainline 板块最高板;      # 与他题材天然互斥
if 卡位 存在: roles[卡位.code]="卡位"
反包列表 = 昨炸板今回封, 过滤掉 code 已在 roles 的   # ← 新增：反包可能与总龙/中军/卡位重叠
genFengCount = mainline板块涨停 - 板块内已占角色的股票数, 下限0   # ← 计数防负
```

**两个补丁点（写代码时落在 `PrdMetricsService.aggregate`）**
1. **反包去重**：当前 `fanBao` 直接收集所有「昨炸板今回封」，可能与 卡位/总龙/中军 重复。改为跳过已占角色。
2. **计数防负**：`genFengCount = mainZt - 板块内占用角色数` 若为负归 0（`mainZt` 按 mainline 板块口径）。

> 若「瑞尔特 总龙头+跟风」来自**手工复盘文档链路**（`ReviewDocFormatter` 用人工 `DailyRecord.leader`），则与本算法无关——请截图确认其来源页后再决定是否另修（§6 决策点）。机器快照链路（`MainlineService`）不会复现该重复。

---

## 4. D2 封顶语义（字段层先行约定，实现另排期）

现状 `applyThemeMainGuards` 仅在 `raw > cap` 时压缩，且无「无主线」档。双轨后：

| 情形 | D2 评分对象 | 封顶 | 打标 |
|:--|:--|:--|:--|
| 有主线(≥3天) | 主线 | 生命周期：确认70/扩散85/亢奋100/退潮30 | 无 |
| 有主线(人工标记) | 主线 | 生命周期（同左） | `manuallyMarked=true` → 🏷人工 |
| **无主线(<3天 且 无人工标记)** | radarTop | **40**（替代萌芽50档） | ⚠️无主线，仅日内核心炒作 |

> `STAGE_CAP` 的「萌芽50」语义将调整：萌芽仍可存在（如主线成立但天数不满足确认），但当 `persistenceDays<3` 且当日无 `t_mainline_mark` 时触发无主线，改封 40。字段 `hasMainline/mainlineSignal/manuallyMarked` 先给出，供前端与后续 Java 一致判断。

---

## 5. 与其它页面分工（沿用你方案 §5，字段支撑已齐）

| 页面 | 内容 | 对应字段 |
|:--|:--|:--|
| **mainline（双轨）** | 主线D2+生命周期+龙头 + 雷达区排名 | 本稿 | 
| **每日复盘 D2 区** | 摘要卡片（主线名+阶段+D2分+当日最强TOP3）→ 跳转 | 由 `mainline` 端点裁剪（服务端出 D2 区摘要子集） |
| **连板生态 D3** | 接力效率，点击高位→D5 | 不动 |
| **D5 高位生态** | 阵眼/抱团/监管（5板以上） | 不动 |

---

## 6. 决策记录（已确认）

| # | 决策点 | 结论 | 影响 |
|:--|:--|:--|:--|
| 1 | 「点击升级到主线区」是否落库 | **落库人工标记**（方案 B） | 新增 `t_mainline_mark` 表 + `/promote` 写接口；人工标记优先级高于 ≥3天自动主线；无主线封顶40 的判定需排除「有当日人工标记」的情形（§2.3/§4） |
| 2 | 「瑞尔特重复」来源 | **跳过单独排查** | 仅按 §3 龙头去重两个补丁点处理（反包过滤 + 计数防负），不在其它链路另行追查 |
| 3 | 雷达区排序 | **zt 降序 → persistenceDays 降序**，不增加成交额切换 | 雷达区 `radar` 列表排序固定为 `zt desc, persistenceDays desc` |

> 已全部落地到正文：§1.1 flag 增 `MANUAL` 展示、§1.2 增 `manuallyMarked`、§2.3 写接口与表结构、§4 封顶语义含人工标记分支、§2.4 排序沿用。

---

## 7. 落地顺序（评审通过后）

**后端**
1. schema 回放：新增 `t_mainline_mark`（§2.3 DDL），先 DELETE 前置幂等。
2. `PrdMetricsService`：抽公共 `consecutiveDays`；加 `radar` 收集与 `radarTop/mainIndustry` 评分对象选择（≥3天最强者→mainline，否则 radarTop）；龙头反包去重 + 计数防负（§3）。
3. 新增 `MainlineMark` entity + `MainlineMarkMapper`；`MainlineService` 组装 `hasMainline/manuallyMarked` 与评分对象优先级（人工标记 > ≥3天自动）。
4. `MainlineVO`：加 `RadarRow` 列表 + `hasMainline/manuallyMarked/mainlineSignal/radarTopIndustry`。
5. `PrdController`：加 `POST/DELETE /api/prd/mainline/promote`（§2.3）。
6. 单测：`RadarRowTest`（N=1/N=2/N≥3/人工标记）、`MainlineObjectSelectionTest`（radarTop≠mainline 时 D2 评 mainline；人工标记优先于自动）、`LeaderDedupTest`（反包与总龙重叠去重）、`MainlineMarkServiceTest`（promote/取消后 hasMainline 翻转）。

**前端**
7. `MainlineView.vue`：双轨布局（主线区 + 雷达区表）+ 走势图改「D2主线明确度走势」+ 升级/取消按钮调 §2.3。
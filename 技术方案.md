# A 股短线情绪周期仪表盘 — 技术实施方案

## Context

用户有一套完整的 A 股短线情绪周期知识库（7 篇文档，涵盖情绪七阶段、六维指标、温度计打分模型、节点理论、操作策略等），需要一个 Web 系统把这套框架变成每天可执行、可积累、可回看的可视化工具。

**核心目标**：每天收盘后 5 分钟完成数据录入 → 自动生成温度曲线 + 阶段定位 + 操作建议 → 长期积累形成周期直觉。

---

## 实施状态（2026-09-06）

Phase 1–4 的代码全部落位在本目录的 `emotion-server/` 与 `emotion-web/`，且**已用真实行情端到端验证过**：JDK 8 + Maven 编译通过、后端单元测试 128 条全绿、MySQL 建库、Spring Boot 跑在 8080、Vite 跑在 5173，六个页面（仪表盘 / 每日复盘 / 主线龙头 / 异动监管 / 节点理论 / 历史回看）都过了浏览器断言。下文各章节的"预计 X 天"是当初的排期估算，不是待办状态。

**第四节（打分与阶段）已经和本文档不一致，以下述为准**：

- 维度从七维扩到**九维**：第 2 维由"昨日涨停溢价"换成**分档溢价合成**（低/中/高三组按 1 : 1.5 : 2.5 加权，空档权重摊给在场档）；新增**第 8 维 阵眼反馈**（`t_anchor`，当前是哈药股份）和**第 9 维 监管股今日溢价**（`t_surveillance`，只 SEVERE+EXCH 进分）。
- 每维取值 **-1 ~ 3 或 null（未评）**，分母仍是 `3 × 已评维数`，所以温度区间是 -33.3 ~ 100。负档只开在原文最低档是一整块的维度上；量能、主线、阵眼三维的 0 已是最重判罚，不再往下加。
- **第 4 维是三分支平均**：炸板率(次数)、家数封板率、回封率各出分再取平均，逐日算式落在 `t_daily_record.broken_note`。注意炸板率数的是打开次数，`100% - 炸板率 ≠ 家数封板率`。
- 溢价**低/中/高界线跟着当天最高板活**：`M = round(H/2)`，低 `2..M-1` / 中 `M..M+1` / 高 `≥M+2`，不再是写死的 2-3 / 4-5 / 6+；展示顺序 高→中→低。
- 阶段阈值 15/35/55/80、`DROP_THRESHOLD=12`、少于 5 维不出主阶段，这三条**从原设计至今没动过**。
- 主阶段之下多了子段序号（`stage_seq`，第几个回合）和"反弹"标签（`stage_phase`）。

**其他与本文档的偏差**：

- 前端复盘表单不含 `heightTrend`（高度趋势）和 `volumeStatus`（量能状态）两个字段。这两项由 `TemperatureCalculator` 对比历史记录自动推导，让用户手填反而会和算法冲突。
- `application.yml` 去掉了 MyBatis-Plus 的逻辑删除配置（`logic-delete-field: deleted`），表里没有 `deleted` 列，保留会导致 SQL 报错。
- 数据源不再是设想中的单一接口：行情走东财三个池子 + 腾讯报价/日 K，全部零第三方 SDK（`HttpFetcher` 自己拼 HTTP）。`t_market_stock` / `t_premium_tier` / `t_surveillance` / `t_stock` / `t_anchor` 五张表是原设计之外加的，之后又补了 `t_index_close` / `t_position` / `t_prediction` 三张（复盘文档的数据源）。
- **导出的那份复盘文档（【一】…【九】），数字来路分三种**（2026-09-07）：
  - **五大指数收盘/涨跌**由 `GET /api/market/snapshot` 自动回补进 `t_index_close`（这张表当缓存，已有 5 行的日子一次腾讯请求都不发），只补它真取到的那几只，不动 md 导进来的行。
  - **上涨/下跌家数**和**我的仓位**是复盘页表单的手动格。前者没有可回查的上游（东财延迟域指数的 `f104/f105` 回 `"-"`，腾讯指数 payload 无全市场 breadth，本地三池名单推不出 5909 只里几家涨），后者本来就不是行情、只有本人知道。
  - **各节判断文字**存 `t_daily_record.doc_notes`（JSON：小节键 → 正文），原样存原样回填、平台不解析，只进这份只读导出；可导入模板的 ```meta 契约里没有它，导入器也不认它。

**本轮（2026-09-08）的六项改动**（计划 `keen-wilderness-otter.md`，除"题材日粒度"一项外都已落地并端到端验过）：

- 仪表盘不再挂阵眼/跨度，也不再重复一份异动监管名单：阵眼表与跨度色带搬到主线龙头页（那页另配一份自己的温度曲线），名单只在 `/surveillance` 一处。
- `/surveillance` 默认只列进分行（SEVERE+EXCH），ZD 例行行收进开关，开关文案同时给出"在列 N 只 · 其中进分 M 只"；新增双轴曲线（左轴进分家数柱、右轴进分溢价线），点某一天展开当日名单。
- 复盘页新增「子项读数（可人工覆盖）」块：第 2 维三组均涨幅、第 4 维家数封板率与回封率、第 8 维阵眼分、第 9 维进分家数与溢价 —— 八条各显示公开读数与算式，填了数的那格**以他填的进分**，清空即退回读数。读数来自新端点 `GET /api/market/score-context?date=`（家数、第 4/8/9 维）与现成的 `GET /api/market/premium-tiers`（三组），**不与 `/snapshot` 合并**：那两维要逐只打日 K，合并会顶穿 `market.budget-ms`，失败形态是"只要七个数却被上游拖成一整屏红"。
- 人工覆盖只存进 `t_daily_record` 的八列 `manual_*`（`DEFAULT NULL` + 实体 `ALWAYS`）。`t_market_stock` / `t_premium_tier` / `t_surveillance` 都不带 `user_id`（每日公开事实，谁复盘都该拿同一个分），人工值一律不落在那三张表里；叠加只发生在 `ScoreContextService.forDate`，`TemperatureCalculator.calculate` 不参与。
- 节点理论的"待验证"从"任何人任何路径都改不动"变成有出口：`GET /api/nodes/{id}/suggest` 给建议状态 + 前置过滤器逐项读数 + 一句来路，判据不齐时 `ready=false` 并列明缺哪一样（不猜）；`POST /api/nodes/{id}/adopt` 由服务端**重算并逐字段比对**后才落库，不一致整条拒绝并回中文原因。
- 顺带修掉三处假话与哑火：响应、注释与 UI 不再说"留空按 0 入库、温度会虚高"（那七列一直是 `DEFAULT NULL`，留空＝整维从分母剔除）；`anchor_note`/`surv_note` 的截断上限按各自列宽分成 295 / 495（原先统一 200，`surv_note` 会被静默砍掉后 300 字）；节点页筛选面板四个读数与 `filter_passed`（读写字段名不匹配，且取的是上一条已存记录的派生值 → 经 UI 建的每条恒为 0）。
- **没做**：主线龙头页的题材日粒度（`t_theme_day`）与删除能力，等单独确认后再动。

**尚未做**：Phase 5 的周期对比、规则引擎动态建议；全市场公告扫描（第 9 维人群目前只覆盖跟踪集合 ≈104 只）；复盘页总龙头/中军仍是手填，未换成可搜索下拉；涨跌家数的当日全市场扫描（要 60 页请求，未做）。

---

## 一、技术选型

| 层 | 选型 | 版本 | 理由 |
|---|---|---|---|
| 前端 | Vue 3 + Vite | Vue 3.4+ | 用户选择 |
| UI 组件 | Element Plus | 最新 | Vue 3 生态最成熟 |
| 图表 | ECharts 5 | 5.x | 温度曲线、sparkline、阶段色带 |
| 后端 | Spring Boot | 2.7.x | 兼容 JDK 8 |
| ORM | MyBatis-Plus | 3.5.x | 轻量、上手快 |
| 数据库 | MySQL | 8.0 | 用户选择 |
| 认证 | Spring Security + JWT | — | 多用户隔离 |
| 构建 | Maven | — | JDK 8 兼容性好 |

---

## 二、项目结构

```
study/股市/code/
├── 技术方案.md                      # 本文档
├── emotion-server/                    # Java 后端
│   ├── pom.xml
│   └── src/main/java/com/emotion/
│       ├── EmotionApplication.java
│       ├── config/                    # Security, CORS, JWT 配置
│       ├── controller/
│       │   ├── AuthController.java        # 注册/登录
│       │   ├── DailyRecordController.java # 每日数据 CRUD
│       │   ├── CycleController.java       # 周期分析
│       │   ├── ThemeController.java       # 主线/龙头追踪
│       │   └── NodeController.java        # 节点理论模块
│       ├── service/
│       │   ├── AuthService.java
│       │   ├── TemperatureService.java    # 温度计打分计算
│       │   ├── CycleService.java          # 周期阶段定位
│       │   ├── DailyRecordService.java
│       │   ├── ThemeService.java
│       │   └── NodeService.java
│       ├── entity/
│       │   ├── User.java
│       │   ├── DailyRecord.java           # 每日指标记录
│       │   ├── Cycle.java                 # 周期（跨多日）
│       │   ├── Theme.java                 # 主线题材
│       │   ├── LeadingStock.java          # 龙头股
│       │   └── NodeEvent.java             # 节点事件
│       ├── mapper/                        # MyBatis-Plus Mapper
│       ├── dto/                           # 请求/响应 DTO
│       ├── vo/                            # 视图对象
│       └── util/
│           ├── JwtUtil.java
│           └── TemperatureCalculator.java # 打分算法
│
└── emotion-web/                       # Vue 3 前端
    ├── package.json
    ├── vite.config.js
    └── src/
        ├── api/                       # 后端接口封装
        ├── views/
        │   ├── Dashboard.vue              # 主仪表盘（温度曲线+六维面板）
        │   ├── CycleView.vue              # 周期定位页
        │   ├── ThemeView.vue              # 主线龙头追踪
        │   ├── NodeView.vue               # 节点理论模块
        │   ├── ReviewView.vue             # 每日复盘工作台
        │   ├── HistoryView.vue            # 历史回看
        │   ├── Login.vue
        │   └── Register.vue
        ├── components/
        │   ├── TemperatureChart.vue       # 温度曲线图（ECharts）
        │   ├── IndicatorCards.vue         # 六维指标卡片
        │   ├── StageLocator.vue           # 阶段定位器
        │   ├── SparkLine.vue              # 迷你趋势线
        │   ├── StageAdvice.vue            # 操作建议面板
        │   └── ReviewForm.vue             # 复盘表单
        ├── stores/                    # Pinia 状态管理
        ├── router/
        └── utils/
```

---

## 三、数据库设计

### 3.1 用户表 `t_user`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | 主键 |
| username | VARCHAR(50) UNIQUE | 用户名 |
| password | VARCHAR(200) | BCrypt 加密 |
| nickname | VARCHAR(50) | 昵称 |
| created_at | DATETIME | 注册时间 |

### 3.2 每日记录表 `t_daily_record`（核心表）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | 主键 |
| user_id | BIGINT FK | 所属用户 |
| trade_date | DATE | 交易日期（唯一索引 user_id+trade_date） |
| **六维原始指标** | | |
| max_consecutive_limit | INT | 连板高度 |
| limit_up_count | INT | 涨停家数 |
| limit_down_count | INT | 跌停家数 |
| yesterday_limit_premium | DECIMAL(5,2) | 昨日涨停今日溢价(%) |
| broken_board_rate | DECIMAL(5,2) | 炸板率(%) |
| big_loss_count | INT | 大面数 |
| total_volume | DECIMAL(10,2) | 两市成交额(亿) |
| **七维打分** | | |
| score_height | TINYINT | 连板高度得分(0-3) |
| score_premium | TINYINT | 溢价得分(0-3) |
| score_breadth | TINYINT | 涨跌停比得分(0-3) |
| score_broken | TINYINT | 炸板率得分(0-3) |
| score_loss | TINYINT | 大面数得分(0-3) |
| score_volume | TINYINT | 量能得分(0-3) |
| score_theme | TINYINT | 主线明确度得分(0-3) |
| **汇总** | | |
| total_score | INT | 温度计总分(0-21) |
| temperature | DECIMAL(5,1) | 换算温度(0-100) |
| prev_temperature | DECIMAL(5,1) | 昨日温度（用于算变化率） |
| **阶段定位** | | |
| stage | VARCHAR(20) | 阶段：冰点/修复/启动/发酵/高潮/分歧/退潮 |
| stage_direction | VARCHAR(10) | 方向：上升/下降/横盘 |
| stage_overridden | TINYINT | 是否手动覆盖阶段(0自动/1手动) |
| **主线龙头** | | |
| main_theme | VARCHAR(100) | 当前主线题材名称 |
| leading_stock | VARCHAR(50) | 总龙头名称 |
| leading_stock_status | VARCHAR(20) | 龙头状态：加速/滞涨/断板/反包 |
| mid_cap_stock | VARCHAR(50) | 中军名称 |
| **复盘文本** | | |
| rotation_note | TEXT | 轮动观察笔记 |
| review_note | TEXT | 对答案笔记 |
| tomorrow_plan | TEXT | 明日计划 |
| **元数据** | | |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 3.3 周期表 `t_cycle`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | 主键 |
| user_id | BIGINT FK | 所属用户 |
| start_date | DATE | 周期起始日（冰点日） |
| end_date | DATE | 周期结束日（下一个冰点前一日，NULL=进行中） |
| max_temperature | DECIMAL(5,1) | 本轮最高温度 |
| max_height | INT | 本轮最高连板 |
| leading_stock | VARCHAR(50) | 本轮总龙头 |
| main_theme | VARCHAR(100) | 本轮主线 |
| status | VARCHAR(10) | ONGOING / COMPLETED |

### 3.4 主线题材表 `t_theme`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | 主键 |
| user_id | BIGINT FK | 所属用户 |
| cycle_id | BIGINT FK | 所属周期 |
| name | VARCHAR(100) | 题材名称 |
| start_date | DATE | 题材启动日 |
| status | VARCHAR(10) | 萌芽/确认/扩散/亢奋/退潮 |
| strength | INT | 强度评分(0-100) |

### 3.5 龙头股表 `t_leading_stock`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | 主键 |
| user_id | BIGINT FK | 所属用户 |
| theme_id | BIGINT FK | 所属题材 |
| name | VARCHAR(50) | 股票名称 |
| role | VARCHAR(20) | 总龙头/中军/跟风/卡位/反包龙 |
| max_consecutive | INT | 最高连板数 |
| start_date | DATE | 启动日 |
| status | VARCHAR(20) | 当前状态 |

### 3.6 节点事件表 `t_node_event`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK | 主键 |
| user_id | BIGINT FK | 所属用户 |
| cycle_id | BIGINT FK | 所属周期 |
| system_type | VARCHAR(10) | A(市场总节点) / B(板块节点) |
| anchor_stock | VARCHAR(50) | 锚定龙头名称 |
| anchor_max_board | INT | 锚定龙头最高板数 |
| d0_date | DATE | D0 日期 |
| d0_candidates | TEXT | D0 候选票(JSON 数组) |
| t1_date | DATE | T+1 验证日 |
| t1_anchor_repack | TINYINT | T+1 老龙是否反包 |
| t1_promotion_count | INT | T+1 晋级数量 |
| t1_promotion_rate | DECIMAL(5,2) | T+1 晋级率 |
| node_valid | TINYINT | 节点是否有效 |
| node_stock | VARCHAR(50) | 确认的节点票 |
| node_stock_max_board | INT | 节点票最高板数 |
| filter_passed | TINYINT | 前置过滤器是否通过 |
| filter_detail | TEXT | 过滤器明细(JSON) |
| status | VARCHAR(20) | 待验证/有效/失效 |
| note | TEXT | 备注 |

---

## 四、核心算法

### 4.1 温度计打分（TemperatureCalculator）

```java
// 根据知识库 03 篇的打分标准
public int calculateTotalScore(DailyRecord record) {
    int score = 0;
    score += calcHeightScore(record);      // 连板高度趋势 (0-3)
    score += calcPremiumScore(record);      // 昨日涨停今日溢价 (0-3)
    score += calcBreadthScore(record);      // 涨停 vs 跌停 (0-3)
    score += calcBrokenScore(record);       // 炸板率 (0-3)
    score += calcLossScore(record);         // 大面数 (0-3)
    score += calcVolumeScore(record);       // 量能/换手 (0-3)
    score += calcThemeScore(record);        // 主线明确度 (0-3)
    return score;  // 满分 21
}

// 温度换算: temperature = totalScore / 21.0 * 100
```

**连板高度得分**需要对比前几日数据（趋势判断），所以打分时需要查询最近 3-5 天的记录。

### 4.2 阶段定位（CycleService）

```java
// 根据温度值 + 变化方向 → 阶段
public String determineStage(double temperature, double prevTemperature) {
    double delta = temperature - prevTemperature;
    
    if (temperature <= 15) return "冰点";
    if (temperature >= 80) return "高潮";
    if (temperature >= 55 && temperature < 80) return "发酵";
    if (temperature >= 35 && temperature < 55) return "启动";
    if (temperature >= 15 && temperature < 35) return "修复";
    
    // 退潮: 温度快速下滑
    if (delta <= -15 && temperature < 50) return "退潮";
    // 分歧: 温度在 60 附近剧烈波动
    if (Math.abs(delta) >= 15 && temperature >= 45) return "分歧";
    
    return "修复"; // 默认
}
```

### 4.3 操作建议生成（静态映射 MVP）

```java
// 阶段 → 操作建议（知识库 05 篇）
Map<String, StageAdvice> ADVICE_MAP = Map.of(
    "冰点", new StageAdvice("空仓观望", "0-1成", 
        "极轻仓试反抽龙头或逆势新题材首板", 
        "严禁抄高位股底部"),
    "修复", new StageAdvice("小仓试错", "1-3成",
        "新题材首板试错，敢做新龙头首板",
        "别急上重仓，修复可能被证伪"),
    // ... 其余阶段
);
```

后续升级 B 方案时，在静态建议基础上叠加规则引擎（温度变化率异常、量价背离等）。

---

## 五、前端页面设计

### 5.1 主仪表盘 Dashboard.vue

**布局**（上→下）：
1. **顶部状态栏**：今日日期 + 温度大数字 + 阶段标签 + 较昨日变化
2. **温度曲线图**（ECharts）：
   - 横轴：交易日序列
   - 纵轴：0-100
   - 背景色带：7 阶段对应颜色区间
   - 曲线：温度折线 + 数据点
   - 标注：今日点高亮
   - 时间范围切换：近 20 日 / 60 日 / 本轮周期
3. **六维指标卡片**：6 个卡片横排，每个含当前值 + 趋势箭头 + 得分
4. **阶段定位条**：水平七阶段进度条 + 当前位置高亮

### 5.2 数据录入（集成在 Dashboard 或独立页）

表单字段：
- 交易日期（日期选择器，默认今天）
- 连板高度、涨停家数、跌停家数、溢价(%)、炸板率(%)、大面数、成交额(亿)
- 主线题材、龙头名称、龙头状态（下拉选择）
- 主线明确度打分（0-3 单选）
- 轮动观察、对答案、明日计划（文本域）

提交后自动计算七维打分 + 温度 + 阶段定位。

### 5.3 复盘工作台 ReviewView.vue

- 左侧：今日录入表单（如已录入则显示已填数据可编辑）
- 右侧：自动生成的阶段操作建议 + 温度计得分明细
- 底部：最近 7 天温度趋势迷你图

### 5.4 主线龙头 ThemeView.vue

- 当前主线列表（卡片形式，含强度条）
- 龙头股列表（按角色分组：总龙头/中军/跟风）
- 龙头状态时间线

### 5.5 节点理论 NodeView.vue

- 前置过滤器状态面板（通过/不通过 + 各项指标对照）
- 当前节点事件追踪（D0 → T+1 → T+2 时间线）
- 历史节点事件列表

### 5.6 历史回看 HistoryView.vue

- 日历视图：每天色块按温度着色
- 点击日期弹出当日详情
- 周期对比（可选，P2 阶段）

---

## 六、API 设计

### 6.1 认证

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /api/auth/register | 注册 |
| POST | /api/auth/login | 登录，返回 JWT |

### 6.2 每日记录

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /api/records | 新增每日记录（自动计算打分+温度+阶段） |
| PUT | /api/records/{id} | 修改记录 |
| GET | /api/records/today | 获取今日记录 |
| GET | /api/records/range?start=&end= | 获取日期范围记录 |
| GET | /api/records/latest?days=20 | 获取最近 N 天 |

### 6.3 周期

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/cycles/current | 获取当前周期 |
| GET | /api/cycles | 获取所有周期列表 |
| POST | /api/cycles | 手动标记新周期起点 |

### 6.4 主线龙头

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/themes/current | 当前主线+龙头 |
| POST | /api/themes | 新增主线 |
| PUT | /api/themes/{id} | 更新主线状态 |
| POST | /api/themes/{id}/stocks | 添加龙头股 |

### 6.5 节点理论

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/nodes/current | 当前追踪的节点 |
| POST | /api/nodes | 新增节点事件 |
| PUT | /api/nodes/{id} | 更新节点状态（D0→T+1→确认） |

### 6.6 统计

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/stats/temperature-summary | 温度曲线数据（给 ECharts 用） |
| GET | /api/stats/stage-distribution | 各阶段天数统计 |

---

## 七、分期实施计划

### Phase 1: MVP（核心闭环）— 预计 3-4 天

**后端**：
1. Spring Boot 项目初始化 + MySQL 建表
2. 用户注册/登录（JWT）
3. DailyRecord CRUD + 温度计自动打分
4. 阶段定位算法
5. 温度曲线数据 API

**前端**：
1. Vue 3 项目初始化 + Element Plus + ECharts
2. 登录/注册页
3. 数据录入表单
4. 主仪表盘（温度曲线 + 六维卡片 + 阶段定位条）

### Phase 2: 复盘 + 历史 — 预计 1-2 天

**后端**：
1. 复盘笔记存储（已含在 DailyRecord 中）
2. 历史数据查询 API

**前端**：
1. 复盘工作台页面
2. 历史日历视图
3. 日期详情弹窗

### Phase 3: 主线龙头 — 预计 1-2 天

**后端**：Theme + LeadingStock CRUD
**前端**：主线龙头追踪页面

### Phase 4: 节点理论 — 预计 1-2 天

**后端**：NodeEvent CRUD + 前置过滤器自动校验
**前端**：节点理论独立模块页面

### Phase 5: 进阶（按需）

- 周期对比
- 操作建议动态化（规则引擎）
- 数据导入导出
- 行情 API 自动填充

---

## 八、验证方案

1. **后端**：每个 Service 写单元测试，验证温度计打分和阶段定位算法的正确性
2. **前端**：启动 dev server，手动验证：
   - 录入一条记录 → 温度曲线出现新点
   - 连续录入多天数据 → 曲线连贯、阶段标注正确
   - 多用户登录 → 数据隔离
3. **端到端**：用知识库中的案例数据（如 8/31 深中华A 节点）录入验证

---

## 九、关键文件清单

| 文件 | 说明 |
|---|---|
| `emotion-server/pom.xml` | Maven 依赖（Spring Boot 2.7, MyBatis-Plus, JWT, MySQL） |
| `emotion-server/.../util/TemperatureCalculator.java` | 温度计打分核心算法 |
| `emotion-server/.../service/CycleService.java` | 阶段定位逻辑 |
| `emotion-server/.../entity/DailyRecord.java` | 核心实体 |
| `emotion-web/src/views/Dashboard.vue` | 主仪表盘 |
| `emotion-web/src/components/TemperatureChart.vue` | 温度曲线图组件 |
| `emotion-web/src/components/IndicatorCards.vue` | 六维指标卡片 |
| `emotion-web/src/components/StageLocator.vue` | 阶段定位器 |

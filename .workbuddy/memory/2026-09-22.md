# 2026-09-22

## 启动失败：application.yml 被写入字面 `/n`（已修）
- 现象：Spring Boot 起不来，`ScannerException: while scanning a simple key ... line 84 daily-bar:/n    # 关掉... could not find expected ':'`。
- 根因：09-21 加 `market.daily-bar` 配置时用的是 `node -e "..."` 内联脚本，**字符串里的 `\n` 被 shell 转义链吞掉，写进文件的是字面 `/n` 两个字符**（正斜杠 + n，不是反斜杠 n）。
  结果 `daily-bar:` 与后面的注释挤在同一行，YAML 把它当成一个没有 `:` 结尾的 key，下一行 `enabled: true` 就成了意外 token。
- 修法：脚本改成 Write 落盘再执行，把 `  daily-bar:/n    # 关掉` 换回 `  daily-bar:\n    # 关掉`；已用 pyyaml 全量解析校验通过，`market.daily-bar` 五个键（enabled/lead-days/volatile-days/max-age-days/volatile-ttl-ms）齐全。
- 教训（长期）：**凡是往文件里写多行文本，一律用 Write 工具落 .js 脚本再跑，不要用 `node -e "..."` 内联带 `\n` 的字符串**——本机 Git Bash 的转义链会把 `\n` 变成 `/n`，且这种错误不会在编译期暴露，只在运行时炸。Java 文件那批因为编译+418 用例全过才没出事，纯文本/配置类文件没有这层保护。

## WaveRider × 情绪温度 联合回测（PRD v1.1 → v1.2）

### 1. 先修掉一个硬错误：§12.2 的 4 个"失败样本"数字全错
用户引用的那组数字（锡华 −3.62% / 新宏泽 −3.86% / 经纬 −5.83% / 科森 −0.56%）是上一轮写进 PRD 的，**与库内权威记录不符**。
双源复核（`t_zt_perf` 的 `source=QUOTE` 行 + 腾讯 qfq 日 K 逐只算收盘涨跌幅）完全一致，正确值是：

| 个股 | PRD 原值 | 正确值 | 说明 |
|---|---|---|---|
| 经纬股份 301390 | −5.83% | **−6.69%** | 12 只里唯一重挫 |
| 锡华科技 603248 | −3.62% | **−1.30%** | 4 板高位 |
| 科森科技 603626 | −0.56% | **−0.30%** | 基本平盘 |
| 新宏泽 002836 | −3.86% | **+0.50%** | **未连板但收红，根本不是失败样本** |

更正后统计：8/12 晋级（66.7%）、平均涨幅 **+6.01%**（原写 +5.50%）、盈亏比 **3.23**（均盈 8.93% / 均亏 2.76%，原写 2.88）。
- 教训：**复盘类数字必须双源复核**。`t_zt_perf`(QUOTE) 与腾讯 qfq 日 K 是两个独立口径，吻合才敢写进文档；凭印象或单源推算会造成整段结论失真（这次直接导致"4 只失败"被夸大）。

### 2. 回测口径与结果（226 样本 / 19 个交易日 / 153 只去重）
口径：T 日 `t_market_stock` 涨停池中 `consecutive ≥ 2` 的票 → T+1 涨跌幅走腾讯 qfq 日 K `close(T+1)/close(T)−1`；T 日温度取 `t_daily_record.temperature`。无未来函数（T 日温度收盘后才算出）。

- **方差分解：日间（选时）11.0% / 日内（选股）89.0%**。温度是市场级同步变量，同日全部候选共享一个取值，**结构上不可能区分个股** → 温度只能当仓位旋钮，不能当选股因子；反过来个股评分也替代不了那 11%。
- 温度分档（**按天**统计，避免同日样本伪独立）：T<55 → 4 天 +2.16%/胜率61.0%；55–60 → 2 天 +1.59%/48.7%；60–70 → 6 天 +3.75%/68.0%；≥70 → 2 天 +5.84%/83.7%。方向单调但每档仅 2–6 天，**不构成可用规律**。
- 反例：温度最低的 09-04（42.5°）次日 **+7.19%、胜率 85.7%**，是全样本最好的一天 → 「低温就不做」是错的（冰点期赔率最高）。
- **温度没跑赢自己的原料**：温度与涨停家数相关 **+0.674**；而涨停家数单独用 vs 次日均涨 Pearson **+0.396**，温度只有 **+0.280**。
- 组合回测（每日等权买当日全部候选，次日收盘卖；信号检验，非可实现收益）：恒满仓 +56.59% → T≥60 才做日均从 +3.26% 升到 +4.24%，累计却掉到 +39.52%。**温度过滤提高单日质量、降低总收益**。
- 单因子 IC（n=226）：**封单/成交额 +0.308（最强）** ＞ 成交额 **−0.285（负向，越小越好）** ＞ 封单额 +0.222 ＞ 首次封板时间 +0.196 ＞ 情绪温度 +0.131 ＞ **连板数 +0.100（最弱）** ＞ 宽度分 b +0.001。高度分 h+溢价分 p 为 **−0.159（反向）**。
  → 直接推翻 `filter_min_amount = 1.0 亿`（方向做反了，已改 0.5 亿 + `MARK`）；连板数几乎无区分度，正是"单靠身位高度不足以过滤风险"的量化证据。

### 3. PRD 落地（v1.1 → v1.2，91675 → 113443 字节）
- 新增 **§12.4**（8 个小节：日级明细 / 温度分档 / 方差分解 / 与原料对照 / 单因子 IC / 组合回测 / 09-18 逐只对照 / 对其他章节的影响）。
- 新增 **§6.2 callout**：一级因子改「封单强度 > 成交额(负向) > 连板数 > 封板时序」，温度不进个股评分。
- 新增 **§13 F15**：当日无温度记录时候选池照常产出，`suggest_position` 置空标 `NO_TEMPERATURE`，**不得按 0 仓位或退潮处理**（实测 19 天只有 14 天有温度，温度含人工填报子指标）。
- **§3.2** 数据源加 `t_daily_record`；**§17** `filter_min_amount` 修正；**§0.3** 加 C8/C9；更正 §12.2 表格与 AC-8；版本号/日期 chips 更新。
- 回测脚本归档到 `prd/短线连板周期选股策略引擎/回测脚本/`（00 候选池抽取 / 01 日K拉取 / 02 联合回测 / 03 日级聚合与组合 / 04 单因子 IC + 样本明细 json），可原样复跑复算。

### 4. 环境再踩一次：`python -c` 里的反斜杠也会被吞
`python -c "... os.path...replace('\\\\','/') ..."` 经 Git Bash 转义链后变成 `'\'` → `SyntaxError: unterminated string literal`。
与 09-22 早些时候的 `node -e` 是同一类问题。**结论统一为：凡多行或含反斜杠/引号的脚本，一律 Write 落文件再执行，不用内联。**

### 5. 数据缺口（回测顺带发现，需回填）
- `t_market_stock` **缺 09-15**（周二）三池数据：09-14 的候选只有涨幅、没有"次日涨停"判定。
- `t_daily_bar` 只有 10 只阵眼票，全市场回测取不到，故本次 153 只票的日 K 是脚本现拉腾讯 qfq（未落库）。
## 上云改造：脚本化部署
- 新增 scripts/deploy/：server-init.sh(一次性装 JDK8/nginx/docker-mysql5.7/目录/systemd/nginx)、build.sh(归集 dist-deploy/{emotion-server.jar,web})、deploy.sh(上传+软链切版本+健康检查+自动回滚+保留5版)、emotion-server.service、nginx-emotion.conf。
- 新增 .github/workflows/deploy.yml（方案三，push main 自动构建+发版，需 secrets: ECS_HOST/ECS_USER/ECS_SSH_KEY）。
- 后端新增 application-prod.yml + logback-spring.xml；SecurityConfig 的 CORS 白名单外置成 emotion.cors.origins（原来写死 localhost:5173，生产会 403）。
- .gitignore 加 dist-deploy/。

## 空间破局节点（space_break）专项 PRD —— 节点体系第四类
- 产出 `../../prd/节点追踪/空间破局节点-PRD.html`（v0.1 草案，57KB），标定脚本归档 `回测脚本/05_空间破局节点标定.py` + `_sb_result.json`。
- **定义经用户两次修正后定稿**：破局日 = **空间板断板日 = 观察日（0 候选）**；破局次日 = 破局日的下一交易日 = 修复日 = **唯一出手日**。不是「突破前高那天」，也不是「连板创新高那天」。（我第一版按「board > 近5日前高」做，是错的，已废）
- 破局日三条件：C1 前一日有**唯一**空间板且 ≥5 板 / C2 当日断板 / C3 情绪未崩（涨停≥40 且 跌停≤10 且 炸板率≤45%）。断板形态三分（DT池=A杀→否决；ZB池=炸板断板；三池均无=温和断板），**按池归属即可判定，无需日 K**。
- 破局次日三条件：R1 破局股次日未跌停 / R2 涨停≥破局日×0.8 且 炸板率恶化≤+5pp / R3 有 3~5 板承接身位。
- **19 日真实标定**：空间板断板 8 次 → 破局日 5 天（08-26 汉森5板炸板断 / 09-02 海鸥7板 / 09-08 龙版6板炸板断 / 09-17 闽东6板 / 09-18 澳弘5板），排 3 天（08-31 跌停11、09-04 涨停39+炸板55.2%、09-10 涨停35+跌停11）。破局次日仅 **3/5 成立**（08-27 / 09-18 / 09-21），**09-03（炸板率22.4→42.9%）与 09-09（涨停73→48 腰斩）修复失败**。
- 四档：L1 承接身位（3~5板唯一最高）/ L2 身位跟随（仅 L1 买不到时）/ L3 新支线首板（题材≥3涨停、昨日≤1）/ L4 独立无板块首板（默认过滤）。真实边界：08-27 最高身位 6 板 > max 5 → **L1 空档**；09-18 华瓷/锡华同为 4 板并列 → 取首封早者、**不降级进 L2**。
- 权重定案：start 1.0 > **space_break_next 0.9** > switch 0.8 > diverge 0.6 > space_break 0（观察日）。破局次日仓位：单票 ≤3%、总仓 30%、scale 0.7。
- **09-18 是最关键的边界样例**：09-17 闽东断板 → 09-18 是破局次日，但 09-18 澳弘 5 板也断 → 09-18 又是破局日。连续断板＝退潮，规则定为**同位时破局日优先、0 候选、顺延不补发**（F18）。另 08-27/09-21 是「破局次日∩切换日」→ 取 0.9 的破局次日、切换日候选并入 L3。
- **V33 已被 `V33__init_tdx_industry_concept_data.sql` 占用**（PRD 初版误写 V33，已改 **V34**）。V28=position_quantity、V31=日K缓存、V32=qfq版本。V29/V30 仍是 PRD 预留空缺。

## 破局节点如何融入现有「节点追踪」页（已写进 PRD §15）
- 现有节点页 = `emotion-web/src/views/NodeView.vue`（991行，路由 `/nodes`，菜单「节点追踪」在 Layout.vue:37-40），数据只有 `t_node_event`，分类只有 `systemType=A/B`（市场总节点/板块节点），**没有任何节点类型枚举**（代码里 node_type 0 命中，只存在于 PRD）。
- **结构同构**：t_node_event 本身就是「D0 → T+1 → 确认」两日验证结构，破局日=D0（观察）、破局次日=T+1（出手）天然匹配 → 最小方案是加 `node_type` 列做成一种节点类型，**不要新建页面**。
- **免费的一致性**：现有前置过滤器第4条是「连板高度未连续压缩 3 天」（`HEIGHT_COMPRESS_DAYS=3`，NodeSuggestService.java:53-64 常量区），破局日恰恰是高度被压缩那天 → `filter_passed=0` → **「破局日不出手」不需要写新逻辑**。阈值验证：3 个成立的破局次日涨停 77/78/103 全过 `MIN_LIMIT_UP=60`。
- 阈值 40 vs 60 **不冲突**：40=识别高度事件，60=判断节点是否值得出手，**别合并成一个常量**。
- 三层分期：L0 加 5 列+页面标签 / L1 SpaceBreakService 自动识别+排班写迁移 / L2 NodeSuggestPanel 加档位列+顶部破局状态条。
- 前端坑：NodeView 时间线是**自绘 div**（`.step-dot.d0/.t1/.t2`，颜色在 NodeView.vue:911-915 **非 scoped** 样式区），不是 el-timeline；新增颜色要走 App.vue 全局语义变量，别硬编码。
- 两套周期口径并存（NodeView 六态 EVOLVE vs utils/stages.js 四带）→ **破局不加进色带**（它是事件不是阶段），另开议题。
- C-4 待定（Q11）：主 PRD 说 t_node_event 与 t_node_detect 两套独立互不写入，但破局日是自动识别的 → 建议 L0/L1 先落 t_node_event，等 V29 策略主表落地再启双写。
- 阻断项 R7：本次标定时**外网行情接口不可达（腾讯 qfq 返回 HTTP 501，沙箱内外都一样）**，四档 T+1 收益只跑出 L2(n=23/+3.81%)、L3(n=5/+7.04%)、L4(n=48/+1.51%)，**L1 零覆盖**。已在 PRD §5.6 显式声明「排序是设计决策不是回测结论」，收益补全前不得以完整选股模式上线。缓存 `_sb_kline.json` 仅 287/718 只有 K 线，别当成全覆盖。
